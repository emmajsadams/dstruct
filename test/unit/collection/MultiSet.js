describe("MultiSet", function () {
    beforeEach(function () {
    });

    it("Should have a number after added", function () {
        var multiSet = new tsds.collections.MultiSet();
        multiSet.add(4);
        expect(multiSet.has(4));
    });

    it("Should have a ");
});
